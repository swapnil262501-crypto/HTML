#include <stdio.h>
int main() {
 char a;
 printf("enter the value of a:");
    scanf("%c", &a);
    if (a = (int) a) {
        printf("The value of a is an integer.\n");
    } else {
        printf("The value of a is not an integer.\n");
    }

}